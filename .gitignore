# Quarto
/.quarto/
*_files/
.DS_Store

# Claude Code local settings (not part of the site)
.claude/

# Editor / OS cruft
*.swp
Thumbs.db

# Rendered site output. GitHub Pages is now built & deployed by the
# "Publish site" Action (source: GitHub Actions), so /docs is no longer
# tracked — CI renders it on each push. It still exists locally for previews.
/docs/

**/*.quarto_ipynb
